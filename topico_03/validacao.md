
## URL testado
http://localhost
Utilizei tambem o hostame - I PARA VER O MEU IP

## Forma de validação
Navegador (Firefox) e comando `curl` no terminal.

## Resultado do teste
O serviço respondeu corretamente com o código de estado HTTP `200 OK`, carregando toda a estrutura HTML da página da loja sem erros.

## Evidência
* **Terminal (curl):** Ao executar o comando `curl -I http://localhost`, o servidor Nginx devolveu o cabeçalho `HTTP/1.1 200 OK`, confirmando que>
* **Navegador:** Foi tirada uma captura de ecrã (print screen) da janela do navegador Firefox acedendo a `http://localhost`, exibindo o título "S>


